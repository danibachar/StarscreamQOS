import XCTest
@testable import StarscreamQOS

final class StarscreamQOSTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(StarscreamQOS().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
